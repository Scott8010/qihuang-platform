# 岐黄智脑商业化平台 — 测试依赖
# CI/CD 流水线专用

# 项目核心依赖（与生产一致）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
bcrypt>=4.0.0
httpx>=0.27.0
pyjwt>=2.9.0

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-xdist>=3.6.0      # 并行测试

# 代码质量
coverage>=7.6.0

# 数据库驱动（CI 环境 PostgreSQL + Redis）
psycopg2-binary>=2.9.0
redis>=5.0.0

# FastAPI 表单解析（testclient 需要）
python-multipart>=0.0.9
